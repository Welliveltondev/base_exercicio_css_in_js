import { FormDiv, FormPrincipal, HeroTitle } from './styles'

const Hero = () => (
  <FormPrincipal>
    <FormDiv>
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </FormDiv>
  </FormPrincipal>
)

export default Hero
